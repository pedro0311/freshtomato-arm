--- dnsmasq/src/dnssec.c	2018-05-10 12:26:25.000000000 +0200
+++ dnsmasq/src/dnssec.c	2018-09-26 15:47:48.000000000 +0200
@@ -876,7 +876,10 @@ int dnssec_validate_ds(time_t now, struc
   
   if (rc == STAT_INSECURE)
     {
-      my_syslog(LOG_WARNING, _("Insecure DS reply received, do upstream DNS servers support DNSSEC?"));
+      if ((strcmp(name, "com") != 0) && (strcmp(name, "COM") != 0))
+        my_syslog(LOG_WARNING, _("Insecure DS reply received from %s"), name);
+      else
+        my_syslog(LOG_WARNING, _("Insecure DS reply received, do upstream DNS servers support DNSSEC?"));
       rc = STAT_BOGUS;
     }
   
