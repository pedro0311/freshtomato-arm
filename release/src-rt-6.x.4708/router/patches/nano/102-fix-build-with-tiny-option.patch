--- nano/src/move.c	2017-08-27 09:50:52.000000000 +0200
+++ nano/src/move.c	2017-10-11 13:25:16.000000000 +0200
@@ -117,6 +117,7 @@ void do_page_up(void)
 
     /* If we're not in smooth scrolling mode, put the cursor at the
      * beginning of the top line of the edit window, as Pico does. */
+#ifndef NANO_TINY
     if (!ISSET(SMOOTH_SCROLL)) {
 	openfile->current = openfile->edittop;
 	openfile->current_y = 0;
@@ -124,6 +125,7 @@ void do_page_up(void)
 	leftedge = leftedge_for(openfile->firstcolumn, openfile->edittop);
 	target_column = 0;
     } else
+#endif
 	get_edge_and_target(&leftedge, &target_column);
 
     /* Move up the required number of lines or chunks.  If we can't, we're
@@ -148,6 +150,7 @@ void do_page_down(void)
 
     /* If we're not in smooth scrolling mode, put the cursor at the
      * beginning of the top line of the edit window, as Pico does. */
+#ifndef NANO_TINY
     if (!ISSET(SMOOTH_SCROLL)) {
 	openfile->current = openfile->edittop;
 	openfile->current_y = 0;
@@ -155,6 +158,7 @@ void do_page_down(void)
 	leftedge = leftedge_for(openfile->firstcolumn, openfile->edittop);
 	target_column = 0;
     } else
+#endif
 	get_edge_and_target(&leftedge, &target_column);
 
     /* Move down the required number of lines or chunks.  If we can't, we're
