--- libpng/scripts/libpng-config-body.in
+++ libpng/scripts/libpng-config-body.in
@@ -83,6 +83,7 @@ while test $# -gt 0; do
 
     --static)
         R_opts=""
+        libs=${all_libs}
         ;;
 
     *)
