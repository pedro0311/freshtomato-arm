--- busybox/Makefile.flags
+++ busybox/Makefile.flags
@@ -60,7 +60,7 @@
 
 ifneq ($(CC),clang)
 # "clang-9: warning: argument unused during compilation: '-static-libgcc'"
-CFLAGS += $(call cc-option,-static-libgcc,)
+CFLAGS += $(call cc-option,-shared-libgcc,)
 endif
 
 CFLAGS += $(call cc-option,-falign-functions=1,)
